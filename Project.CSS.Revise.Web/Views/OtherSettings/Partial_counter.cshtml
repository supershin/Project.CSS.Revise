
<style>
    :root {
        --brand-green: #c9f0cf;
        --brand-green-text: #1f7a3e;
        --brand-purple: #ecd9ff;
        --brand-purple-text: #6f3acb;
        --card-border: #e7ecef;
        --chip-red: #ffdfdf;
        --chip-red-text: #b44b4b;
        --chip-amber: #ffecc7;
        --chip-amber-text: #8a5a00;
        --btn-teal: #167a7a;
        --btn-teal-hover: #0f5b5b;
    }

    /* Section title pill */
    .section-pill {
        display: inline-block;
        padding: .55rem 1rem;
        border-radius: 12px;
        font-weight: 700;
        font-size: 1.05rem;
    }

    .pill-green {
        background: var(--brand-green);
        color: var(--brand-green-text);
    }

    .pill-purple {
        background: var(--brand-purple);
        color: var(--brand-purple-text);
    }

    /* Yellow / warning pill (uses your existing variables) */
    .pill-amber {
        background: var(--chip-amber);
        color: var(--chip-amber-text);
        border: 1px solid #ffe29a; /* optional subtle edge */
    }


    /* Cards */
    .proj-card {
        position: relative;
        border: 1px solid var(--card-border);
        border-radius: 14px;
        padding: 1.25rem 1.25rem 1rem;
        background: #fff;
        transition: box-shadow .15s ease, transform .05s ease;
        height: 100%;
    }

    .proj-card:hover {
        box-shadow: 0 8px 20px rgba(0,0,0,.06);
        transform: translateY(-1px);
    }

    /* top-right placement + color variants for your existing .count-badge */
    .count-badge.is-top-right {
        position: absolute;
        top: 10px;
        right: 12px;
    }

    .count-badge.is-purple {
        background: var(--brand-purple);
        color: var(--brand-purple-text);
    }
    /* purple */
    .count-badge.is-amber {
        background: var(--chip-amber);
        color: var(--chip-amber-text);
    }
    /* warning */

    .site-name {
        font-weight: 800;
        font-size: 1.1rem;
        margin: 0;
    }

    .subtype {
        font-size: .85rem;
        padding: .25rem .55rem;
        border-radius: 8px;
        display: inline-block;
        background: #f2f7f5;
        color: #5b6b66;
        border: 1px dashed #dbe6e1;
    }

    .chip {
        display: inline-block;
        padding: .25rem .55rem;
        font-size: .8rem;
        border-radius: 999px;
        font-weight: 600;
        vertical-align: middle;
    }

    .chip-red {
        background: var(--chip-red);
        color: var(--chip-red-text);
    }

    .chip-amber {
        background: var(--chip-amber);
        color: var(--chip-amber-text);
    }

    /* Right rounded badge */
    .count-badge {
        background: #eaf9ee;
        color: #2f9e44;
        border-radius: 999px;
        padding: .35rem .7rem;
        font-size: .8rem;
        font-weight: 700;
        white-space: nowrap;
    }

    /* Bottom actions */
    .action-btn {
        background: var(--btn-teal);
        color: #fff;
        border: none;
        border-radius: 10px;
        padding: .5rem .75rem;
        font-weight: 600;
    }

    .action-btn:hover {
        background: var(--btn-teal-hover);
        color: #fff;
    }

    /* icon-btn : ใช้ฟอนต์ Prompt แต่ไม่หนา */
    .icon-btn {
        background: #e7f4f4;
        color: #0f5b5b;
        border: none;
        border-radius: 10px;
        padding: .5rem .7rem;
        font-family: "Prompt", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif;
        font-weight: 400 !important; /* <- no bold */
        font-size: 0.95rem;
        line-height: 1.25;
        display: inline-flex;
        align-items: center;
        gap: .35rem; /* แทน margin-right บน <i> */
    }

        .icon-btn i {
            font-weight: 400;
        }
        /* กันฟอนต์หนาจาก parent */
        .icon-btn b, .icon-btn strong {
            font-weight: 400 !important;
        }
    /* กันกรณีมี <b>/<strong> ภายใน */


    .btn-row {
        gap: .5rem;
    }


</style>
<div class="container">
    <!-- ===================== Bank Pre-Approve ===================== -->

    <div class="d-flex gap-2 my-3">
        <button id="btnQrSimple" class="btn btn-outline-secondary">
            <i class="bi bi-eye me-1"></i>Preview Simple QR
        </button>
        <button id="btnQrPretty" class="btn btn-primary">
            <i class="bi bi-download me-1"></i>Download Pretty QC QR
        </button>
    </div>



    <div id="show_hide_bank" class="d-none">
        <div class="mb-3">
            <span class="section-pill pill-green">
                <i class="bi bi-bank me-1"></i>Bank
            </span>
        </div>
        <div class="row g-4" id="bank_cards">
            <!-- dynamic -->
        </div>
    </div>

    <!-- ===================== Inspect ===================== -->
    <div id="show_hide_inspect" class="d-none">
        <div class="mt-5 mb-3">
            <span class="section-pill pill-amber">
                <i class="mdi mdi-clipboard-check-outline me-2"></i>Inspect
            </span>
        </div>
        <div class="row g-4" id="inspect_cards">
            <!-- dynamic -->
        </div>
    </div>


</div>

<partial name="Partial_modal_counter" />
@section Styles {
    <link rel="stylesheet" href="~/css/counter.css?ver=@DateTime.Now.Ticks">
}



